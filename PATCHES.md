# Fork patch manifest

The `gabe/fork` branch is a downstream patch stack on an immutable upstream
`rust-v*` release tag. Fork Fleet owns the logical patch mapping; this file is
the human review surface for intent, supersession decisions, conflict watches,
and release validation. The target paragraph and table below are generated by
`scripts/render_patches_manifest.py` from the immutable Fork Fleet plan and
checked by the `manifest-sync` validation action; edit only the sections
outside the markers.

<!-- forkctl:generated:begin -->
Current upstream target: **`refs/tags/rust-v0.155.1`** (`be2951ea34f0d295ed0becf97079f92fa5f6950e`). Source base: `90854393966b21e9ebfd21b122334eb09a20c93d`.

## Maintained logical patches

| Patch | Class | Decision | Current upstream assessment |
|---|---|---|---|
| `agent-legacy-gone-wait-failure` | agent-session-integrity | drop | The rust-v0.154.0 target already terminates waits deterministically for agents missing before subscription and agents removed while waiting. It returns terminal NotFound state with timed_out=false and marks the collaboration call failed. The downstream commit changes the model-facing representation to a tool error but does not close an additional demonstrated liveness gap. [upstream_replaced] |
| `agent-legacy-stored-child-owner-resume` | agent-session-integrity | drop | Upstream d21794d6ba and d1f14e31a7 route stored V2 children through their recorded live owners and preserve provider provenance, so the legacy resume reconciliation must not be replayed. [upstream_replaced] |
| `residency-idle-timeout` | app-server-residency | drop | The selected rust-v0.154.0 target contains upstream 5e26f7621c1c470fe62350d61c9eb4d6c772a0da's configurable thread-unload delay. The local five-minute value remains a separate source-owned configuration/cutover obligation, not a code backport. [upstream_replaced] |
| `residency-retaining-kind` | app-server-residency | drop | DEFER: The attempted app-server unload path bypassed core Multi-Agent V2 residency and environment ownership and raced fresh child input at zero delay. Correct eviction requires a core-owned lifecycle design and is deferred. [defer_pending_product_decision] |
| `handover-legacy-generation-lifecycle` | generation-handover | drop | DEFER: The legacy handover introduces a second poll-driven lifecycle with unsafe drain admission, detached work, and false writer-release reporting; live handover is deferred until a target-native owner is designed. [defer_pending_product_decision] |
| `history-claude-compaction-epochs` | history-integrity | drop | DEFER: The attempted target-native import retained every compact epoch in model-visible context, causing unbounded growth and duplicate turn identities. A correct repair belongs in native compaction, resume, and inference ownership and is deferred. [defer_pending_product_decision] |
| `history-orphan-output-ordering` | history-integrity | drop | The attempted repair introduced cold-order, append-failure, late-output race, duplicate-ID, and weakened-invariant regressions. Current upstream already owns prompt-only orphan repair; broader persisted ordering is deferred. [ineffective_or_incorrect] |
| `routing-signed-cwd-claims` | local-runtime-policy | apply | The committed target-native sender is absent from rust-v0.154.0 and is required by the separately host-owned proxy verifier. It signs HTTP, WebSocket, and session paths, refreshes when the session CWD changes, and scrubs signing material from child shell environments. |
| `runtime-alternate-home-isolation` | local-runtime-policy | rework | Target review found no upstream equivalent. Rebuild the narrow loader change with full layer-discovery and composition coverage so its strengthened target-native proof is owned by the same leaf. |
| `runtime-daemon-bootstrap-identity` | local-runtime-policy | drop | DEFER: Repeated target-native attempts could leave legacy installers running, orphan process groups, misidentify logical versus resolved binaries, race PID reuse, or restart ready backends. The identity expansion is deferred independently of stock-updater policy. [defer_pending_product_decision] |
| `runtime-daemon-socket-selection` | local-runtime-policy | rework | The retired generation handover bundled a safe socket-selection seam with unsafe lifecycle ownership. DSH's reviewed host attribution proxy needs only this independent seam: resolve an absolute override, preserve the default, reject relative paths, and pass the resolved socket to the spawned backend. |
| `runtime-stock-updater-policy` | local-runtime-policy | rework | Externally managed fork packages remain protected by a narrow target-native policy: bootstrap refuses while a legacy updater is active, never starts the stock updater, preserves explicit backend replacement, and makes remote ensure idempotent only for matching ready state. |
| `runtime-tmp-inode-hygiene` | local-runtime-policy | drop | All three fixtures still consume TempDir guards with keep(); nesting changes top-level directory shape but not tmpfs inode consumption, and no repository-owned reaper contract proves reclamation. [ineffective_or_incorrect] |
| `runtime-uds-rendezvous-security` | local-runtime-policy | drop | Three adversarial reviews found unresolved ancestor substitution, socket-identity serialization, no-follow lock, cleanup, custom-path compatibility, and public API risks. A correct descriptor-relative rendezvous design exceeds this release. [ineffective_or_incorrect] |
| `oauth-reactive-authorization-retry` | mcp-oauth | drop | The selected rust-v0.154.0 target contains upstream 88912c04cd's coordinated OAuth runtime and authorization retry, so the legacy reactive retry must not be replayed. [upstream_replaced] |
| `oauth-slack-response-adapter` | mcp-oauth | rework | If Slack still needs normalization, rebuild it behind explicit provider identity so lookalike non-Slack responses cannot be rewritten. |
| `oauth-token-store-transaction` | mcp-oauth | drop | The selected rust-v0.154.0 target contains upstream 88912c04cd's coordinated credential-store and refresh-runtime fix, so the unsafe legacy persistence path must not be replayed. [upstream_replaced] |
| `oauth-transition-quarantine` | mcp-oauth | drop | The source commit mixes token persistence, reactive retry, and Slack normalization; replaying it would preserve unsafe generic provider detection and hide three distinct failure boundaries. [split_into_named_retained_leaves] |
| `observability-legacy-model-request-spans` | observability | drop | The legacy spans export raw account ID and email and therefore violate the trace-safe identity policy; the replacement is Alloy-side capture of upstream's native codex.sse_event response.completed log event, requiring no fork patch. [ineffective_or_incorrect] |
| `maintenance-canonical-package-validation` | release-mechanics | rework | The registered Bazel release_binaries command is an upstream six-platform RBE matrix and fails locally at proprietary Windows SDK linkage after 6,257 successful actions. Use codex-use-local-build as the single package owner so validation matches the x86_64 Linux artifact actually prepared for deployment and can be reused without rebuilding. |
| `maintenance-generated-release-tail` | release-mechanics | rework | Release metadata, locks, and schemas must be regenerated from the frozen rust-v0.154.0 semantic candidate rather than replayed from 0.150.1. Snapshot changes belong to the semantic UI leaf whose behavior they render. |
| `maintenance-repo-local-upgrade-workflow` | release-mechanics | rework | The repository-local workflow remains required, but this release must incorporate leaf ownership, upstream adjudication, rework finalization, and early coherence gates before reuse. |
| `maintenance-target-bound-format-validation` | release-mechanics | rework | The registered Fork Fleet invocation already injects the immutable candidate target. Standalone validation has no such authority, so it must derive the history target itself and treat a supplied mismatch as an error; only the exact target's unchanged justfile mismatch may be suppressed after every changed-language formatter passes. |
| `maintenance-upstream-test-stabilization` | release-mechanics | rework | The exact-target differential workspace gate isolated candidate-only failures caused by test observation races or an artificially shortened test-only deadline. Keep their fixture-only repairs in one narrow leaf so they share a no-product-behavior boundary and can be reviewed independently from semantic patches. |
| `maintenance-validation-environment-fixtures` | release-mechanics | rework | Keep the permission-profile fixture path adaptation separate from validator policy and target-baseline logic so it can retire independently when upstream tests become environment-independent. Other historical host-isolation hunks remain dropped with the quarantine. |
| `maintenance-validation-transition-quarantine` | release-mechanics | drop | The source commits remain accounted for exactly once here, while four source-free rework leaves own the target-native workflow contract, exact-target format policy, isolated environment fixtures, and upstream-race test stabilization permitted by the leaf-v1 supersession schema. [split_into_named_retained_leaves] |
| `maintenance-workflow-contract-validation` | release-mechanics | rework | Rebuild only the workflow and manifest audit contract from the quarantined validation commits; target-specific formatter adjudication and host-isolated Rust fixtures remain separate leaves. |
| `release-transition-quarantine` | release-mechanics | drop | These commits mix generated output, formatting, obsolete reconnect and handover behavior, and cross-capability repair hunks; retained semantics have dedicated replacement leaves. [split_into_named_retained_leaves] |
| `transport-legacy-turn-replay` | transport-resilience | drop | Upstream owns fresh-client reconnect and intentionally avoids ambiguous user-operation replay; the downstream process-local replay is not generation-durable or atomic. [product_removal] |
| `tui-legacy-reconnect-replay` | tui-resilience | drop | Upstream TUI reconnect now creates a fresh client and resumes the selected thread without ambiguous operation replay; the legacy attachment and generic replay owner is superseded. [upstream_replaced] |
| `tui-legacy-transport-recovery` | tui-resilience | drop | Upstream now owns nonfatal transport recovery through fresh-client TUI reconnect; the command-local legacy path would create a second recovery owner. [upstream_replaced] |
| `tui-server-request-ownership` | tui-resilience | drop | The rust-v0.154.0 target contains 0c14c73471 and 319b2f72b1, which provide stronger app-server request routing, background-session ownership, navigation, and dashboard integration with broader tests than the downstream patch. [upstream_replaced] |
| `tui-steer-overload-retry` | tui-resilience | drop | DEFER: The target-native retry still guarded and popped the globally displayed ChatWidget rather than the originating thread's saved input, allowing thread switches to strand or misroute a steer. Per-thread retry ownership is deferred. [defer_pending_product_decision] |
| `tui-external-status-command-runtime` | tui-status | rework | A dedicated runtime leaf owns process containment, environment policy, debounce, and stale-result rejection independently of wire and TUI presentation. |
| `tui-external-status-config-contract` | tui-status | rework | A narrow config leaf isolates validation and layer policy from process execution and TUI rendering. |
| `tui-external-status-integration` | tui-status | rework | Integration owns only native-state translation and presentation; wire, parser, and process safety remain separate leaves. |
| `tui-external-status-multiline-footer` | tui-status | rework | Footer height and rich presentation are independently reviewable from formatter input construction, scheduling, and process execution. |
| `tui-external-status-rich-output-runtime` | tui-status | rework | Rich output safety is independently reviewable from process containment and footer presentation. |
| `tui-external-status-surface` | tui-status | drop | The 4.3k-line source commit crosses configuration, wire protocol, rich output parsing, process execution, and high-conflict TUI layout. Quarantine it and rebuild those failure boundaries as target-native leaves. [split_into_named_retained_leaves] |
| `tui-external-status-wire-v1-compatibility` | tui-status | rework | Keep wire compatibility independently reviewable and retireable from process execution and TUI presentation. |
| `tui-streamed-message-identity` | tui-streaming | drop | Fresh-client resume plus the replay filter now own target-native streamed-item recovery, so the legacy reconnect-coupled identity state must not be replayed. [upstream_replaced] |
| `workload-legacy-contained-pty` | workload-attribution | drop | Upstream rust-v0.154.0 has a stronger Windows suspended-spawn and job-object containment primitive; replay would duplicate and weaken the platform owner. [upstream_replaced] |
| `workload-mcp-runtime-attribution` | workload-attribution | rework | Inject authoritative reserved workload identity only after connection cache/reuse decisions so configured identity remains stable, configured env cannot spoof ownership, and reuse does not churn the MCP connection. |
| `workload-scoped-command-wrapper` | workload-attribution | rework | Rebuild attribution as a trusted outer launcher applied after sandbox transformation, carry it as a backward-compatible optional exec-server field, and keep original argv authoritative for approval, policy, events, network, and snapshot caching. Session profile derives from the actual Codex home. |
<!-- forkctl:generated:end -->

## Important integration invariants

- `turn/start` replay is allowed only with a non-empty
  `clientUserMessageId`, is bounded, and is not released until owned threads
  reattach after reconnect.
- Drain admission must be exhaustive over `ClientRequest`. New request variants
  must choose both named policy flags: whether they start new work and whether
  they can create an implicit attachment. Existing-turn continuation operations
  such as steering, interrupting, writing to a command/process, and stopping
  realtime remain available while draining.
- Idle auto-attached threads do not count as retaining subscribers; explicit
  clients do. Active threads never unload.
- The stable router owns generation selection. TUI reconnect logic reattaches
  owned threads but does not independently select or promote generations.
- OAuth recovery uses the shared upstream token-store locks. The fork's
  compare-and-delete function must remain behaviorally aligned with the
  upstream unconditional delete path except for its intentional stale-token
  and executor-ownership guards.
- External status-line execution stays bounded and contained. Do not duplicate
  upstream native status rendering or use raw `unicode_width` for TUI cell
  arithmetic.
- App-server protocol changes require stable and experimental schema fixture
  regeneration. The root `write-app-server-schema` recipe invokes
  `app-server-protocol/scripts/write_schema_fixtures.py`.
- Stable release normalization starts from the tag's `Cargo.lock` dependency
  selections. Never use an unconstrained lockfile regeneration to manufacture
  a clean diff.
- Fork Fleet validation runs through `scripts/fork_fleet_validation.py`, which exposes only named
  validation actions and supplies the bounded local toolchain plus checksum-verified V8 artifacts
  without inheriting the Worklens daemon environment.

## Superseded historical patches

The following entries describe historical components or release artifacts
that are no longer independent downstream behavior.

- The old monolithic MCP OAuth patch was superseded by upstream's modular OAuth
  implementation. In `0.149.1`, upstream also supersedes its issuer
  binding/tracking, locked and atomic fallback-token writes, and
  `invalid_grant` classification components. The downstream patch retains only
  stale compare-delete, refresh-token preservation, the host/executor collision
  guard, reactive live-401 retry, and Slack envelope normalization.
- MCP connection-manager reaping was superseded by upstream connection reuse;
  replaying it would tear down active connections.
- The standalone TUI `turn/start` error patch was upstreamed. Its fork-specific
  transport/replay residue lives in the remote resilience and TUI recovery
  patches.
- Intermediate alpha.9 and alpha.12 lock/version restamps and standalone
  rustfmt commits are release artifacts, not maintained behavior.
  `fork-patch-manifest-maintenance` reworks the stable candidate's
  `Cargo.lock` workspace versions and `CARGO_PKG_VERSION` snapshot
  expectations from the target manifest/version and target-tag dependency
  selections, without replaying those alpha commits.

## Per-upgrade verification

1. Verify the recorded source base and target tag by full SHA; do not infer the
   boundary from ancestry between release tags.
2. Review every logical patch for full, partial, or absent upstream coverage.
3. Replay into a fresh Fork Fleet worktree and resolve conflicts by behavior,
   never wholesale `ours` or `theirs` selection.
4. Regenerate config/app-server schemas and review every TUI snapshot change.
5. Validate locked Cargo metadata, Bazel lock parity, argument comments, scoped
   crate tests, and the release build. Run the complete `just test` suite only
   with explicit authorization.
6. Attribute every file in `git diff rust-v0.150.1..HEAD` to a logical patch or
   stable release normalization.
7. Publish a dated safety ref before updating `gabe/fork`, use an exact
   `--force-with-lease`, create the annotated `fork/0.150.1` tag, package the
   exact published SHA, and use natural drain for the live rollout.
8. Regenerate the generated region with `scripts/render_patches_manifest.py --plan <plan.json> --manifest PATCHES.md` and confirm `scripts/fork_fleet_validation.py manifest-sync`, `attribution`, and `schema-fixtures` pass.
